# Local environment variables for frontend (Next.js)
# Copy this file to `.env.local` and fill values. Do NOT commit your real keys.

# YouTube Data API key (server-side). Keep secret.
YOUTUBE_API_KEY=
NEXT_PUBLIC_API_URL=http://localhost:3001
